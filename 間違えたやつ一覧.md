### 問題
あるメディア制作会社は、ビデオエンコードのジョブ進行状況やリソース利用率を詳しく知るために、それらのメトリクスを継続的に監視したいと考えている。そのため、CloudWatchエージェントをすべてのAmazon EC2 Linuxインスタンスにインストールして、エンコードサーバーの詳細な監視を行うことを計画している。現在、この会社は約500のEC2インスタンスを持っているが、インスタンスは異なる設定や用途で使用されている。
この監視要件を効率的に実現するために、どのような対応をすべきか。
1.AWS Configを使用して、CloudWatchエージェントがインストールされていないEC2インスタンスを特定し、手動でエージェントをインストールする
2.Amazon EC2 Auto Scalingグループを利用し、CloudWatchエージェントがプリインストールされたAMIを起動する
3.AWS Systems Manager Run Commandを使用して、すべてのEC2インスタンスにCloudWatchエージェントをインストールする
4.AWS Systems Manager Patch Managerを使用して、すべてのEC2インスタンスにCloudWatchエージェントのインストールを含むカスタムパッチを適用する

### 正解
3
### 自分の回答
4

### 問題
ネット銀行が、モバイルバンキングアプリ取引の管理インフラをAWSへ移行することを検討している。運用チームは、取引の実績管理やデータ分析をKubernetesクラスター上でコンテナ化されたアプリケーションを用いて行っている。また、これらの生データや分析結果はMongoDBで管理されている。移行する際、業務に支障をきたしたくない、また新しい環境での運用作業を増やしたくないとの要望がある。最も適切な移行方法として、どのサービスの組み合わせを選ぶべきか。（３つ選択）
1 Amazon ECSを使用する
2 Amazon EC2インスタンス上でコンテナを実行する
3 MongoDBをAmazon DynamoDBに移行する
4 Amazon Fargateでコンテナを実行する
5 MongoDBをAmazon DocumentDBに移行する
6 Amazon EKSを使用する

### 正解
４、５、６
### 問題
３、４、６

ある企業では、コンテナ化されたアプリケーションの実行にAmazon EKSを利用することを検討している。企業のセキュリティポリシーに基づき、EKSクラスター内に格納されるシークレット（ログイン時の認証情報など）を、AWS KMS（Key Management Service）で管理されているキーを使用して暗号化する必要がある。運用上のオーバーヘッドを最小限に抑えてこのセキュリティ要件を満たすには、どうすれば良いか。
1 EKSクラスター作成時に、シークレットの暗号化オプションを有効にする
2 EKSクラスター作成時に、エンドポイントアクセスの設定をプライベートにする
3 AWS Secrets Managerを使用して、EKSクラスタに格納されるシークレットを暗号化する
4 EKSクラスターで使用するシークレットが格納されるS3バケットのデフォルト暗号化を有効にする

正解
1
自分の回答
3

ある企業は、顧客企業のAWSアカウント内のAmazon S3バケットに保存されたデータを参照して分析するサービスを開発している。サービス提供側の企業からは、顧客企業のセキュリティを維持したまま安全にアクセスする必要がある。この要件を満たす最適な方法はどれか。
1.顧客企業側で、S3バケットに対する読み取り専用権限を持つIAMユーザを作成し、作成したIAMユーザの資格情報をサービス提供企業側に共有する
2.顧客企業側で、S3バケットに対する読み取り専用権限を持つIAMロールを作成し、サービス提供企業側のアカウントがこのロールを引き受けられるように信頼ポリシーを設定する
3.顧客企業側で、S3バケットのデータを公開し、サービス提供側企業からはインターネット経由で直接アクセスする
4.顧客企業側で、Amazon Cognitoを使用してユーザを作成し、このユーザにS3へのアクセス権限を与えるIAMロールを定義する。サービス提供企業はこのユーザを使用してS3バケットにアクセスする

正解
2
回答
4

### 問題
ある金融機関は、顧客に取引アラートを送信するためのアプリケーションをAWS上で開発している。このアプリケーションではLambda関数を利用して、取引情報を含むメッセージをSNSトピックへ発行する予定である。取引情報には顧客の機密性が高い財務情報が含まれているため、セキュリティ要件としてSNSトピックはAWS KMS（Key Management Service）のカスタマーマネージドキーを使用したサーバーサイド暗号化を行う。同金融機関は、Lambda関数がSNSトピックに対して安全にメッセージを発行するための適切な権限を設定したいと考えている。
これらの要件を満たすために必要な手順の組み合わせはどれか。（3つ選択）
1.SNSトピックのリソースポリシーで、Lambda関数のAmazonリソース名（ARN）を指定し、Publish権限を与える
2.Lambda実行ロールに、SNSトピックへのメッセージ発行とKMSカスタマーマネージドキー利用を許可する権限を付与する
3.Lambda関数のリソースベースポリシーで、SNSトピックへのメッセージ発行とKMSカスタマーマネージドキー利用を許可する権限を付与する
4.KMSのAWSマネージドキーのキーポリシーを編集し、メッセージ発行に関与するプリンシパルを追加して権限を与える
5.SNSトピックの暗号化に使うKMSカスタマーマネージドキーのキーポリシーで、メッセージ発行に関与するプリンシパルがキーを利用できるよう権限を付与する
6.SNSトピックのリソースポリシーで、Lambda実行ロールがトピックにメッセージを発行できるよう許可する

### 正解
2,5,6
### 自分の回答
1,3,5

### 問題
大手小売企業は、オンプレミスとAWS上のVPCをDirect Connectで接続したハイブリッド環境を運用している。オンプレミスの新しいシステムは、VPC内に構築されているEC2インスタンスを呼び出すことが必要になった。新しいシステムは、AWS上のリソースに対してプライベートDNSホスト名を使用してアクセスする必要があるが、EC2インスタンスは、セキュリティ上の理由からプライベートサブネットに配置されている。
これらの要件を満たし、最も効率的で管理しやすいソリューションはどれか。
1.VPCピアリングを設定して、オンプレミスからのDNSクエリをVPC内に転送する
2.Route 53 Resolverにインバウンドエンドポイントとリゾルバールールを作成する。リゾルバールールをVPCに関連付ける
3.Direct Connectとは別にVPN接続を設定し、VPCのDNSサーバーをオンプレミス環境に公開する
4.NATゲートウェイを使用して、オンプレミスからのDNSクエリをインターネット経由でVPC内に転送する

### 正解
2
### 自分の回答
4

### 問題
ある製薬会社は、自社アカウント内のVPCから、パートナー企業が管理している重要な研究データにアクセスして、社内での分析に使用する必要がある。パートナー企業は、自身のAWSアカウント内でRDS for Oracleを運用しており、このデータベースには分析に必要なすべてのデータが保存されている。製薬会社のVPCにはインターネットゲートウェイやDirect Connect、VPN接続がなく、さらに、製薬会社とパートナー企業のVPCはCIDRブロックが重複している。両社ともデータのセキュリティを最優先しており、インターネットを介した通信を避けたいと考えている。
製薬会社がパートナー企業のVPC内にあるデータベースにセキュアにアクセスするために、最適な接続方法はどれか。
1.製薬会社のVPCとパートナー企業のVPCをVPCピアリングで接続し、RDS for Oracleにアクセスする
2.パートナー企業が作成したNLBを介して、AWS PrivateLinkを使用してパートナー企業のRDS for Oracleにアクセスする
3.パートナー企業のRDS for Oracleのデータを定期的にS3にエクスポートしてもらい、そのS3バケットに製薬会社がアクセスする
4.製薬会社が、パートナー企業のRDS for Oracleに対してTransit Gatewayを使用して接続し、アクセスする

### 正解
2
### 自分の回答
1

### 問題
ある企業では、プライベートサブネット内のEC2インスタンス上でデータ分析アプリケーションをホストしている。このアプリケーションは、インターネット上に公開されている外部APIのURLに定期的にアクセスして気象情報に関するデータを取得している。同社では現在、ネットワークセキュリティの強化が求められており、インターネットを介した通信については、このAPIとEC2上のアプリケーション間のみ許可したい。これらの要件を満たす方法はどれか。
1.EC2インスタンスのセキュリティグループで、APIのURLとのインバウンド及びアウトバウンド通信のみ許可するように設定する
2.AWS Network Firewallを構成し、APIとの通信のみを許可するドメイン名によるフィルタリングのルールを作成する。プライベートサブネットからのアウトバウンド通信がNetwork Firewallを経由するようにルートテーブルを修正する
3.AWS Shield Advancedを構成し、APIとの通信以外のすべてのネットワークトラフィックをブロックするルールを作成する
4.AWS Firewall Managerを用いて、VPCを出入りするすべてのネットワークトラフィックを検査する。指定したURLとインターネットゲートウェイ間の通信のみ許可するファイアウォールの設定を行う

### 正解
2
### 自分の回答
1

### 問題
ある企業は、Amazon ECSクラスター上で実行されるアプリケーションに対して、複数の特定のS3バケットへの読み取りアクセスのみを、最小権限で許可したいと考えている。セキュリティ要件として、アプリケーションを実行するECSタスク以外のAWSリソースにS3バケットへのアクセス権限を付与してはならない。
これらの要件を満たす最も適切なソリューションの組み合わせはどれか。（2つ選択）
1.ECSのタスク定義でtaskRoleArnパラメータに指定するIAMロールに、作成したIAMポリシーをアタッチする
2.アクセスが必要なS3バケットのARNをIAMポリシーのResource要素に明示し、必要な読み取りアクションのみを許可するIAMポリシーを作成する
3.バケット名にワイルドカード指定を含むIAMポリシーを作成する
4.ECSクラスターを構成するコンテナインスタンスに関連付けられたIAMインスタンスロールに、作成したIAMポリシーをアタッチする
5.ECSのタスク定義でexecutionRoleArnパラメータに指定するIAMロールに、作成したIAMポリシーをアタッチするAMインスタンスロールに、作成したIAMポリシーをアタッチする
6.ECSのタスク定義でexecutionRoleArnパラメータに指定するIAMロールに、作成したIAMポリシーをアタッチする

### 正解
1,2
### 自分の回答
2,3

### 問題
ある会社は、顧客向けの新しいカスタマーサポート用アプリケーションを設計している。顧客からの質問やフィードバックの送信といったリクエストを受け付けるためにREST APIを使用したい。このAPIからは、同社が運用するVPC内のプライベートサブネットに配置されたEC2上のアプリケーションにアクセスする必要がある。また、セキュリティ上の理由により、REST APIからEC2へのアクセスはインターネットを経由してはならない。これらの要件を満たすソリューションとして、最も適切なものはどれか。
1.Amazon API GatewayでREST APIを作成し、EC2のセキュリティグループにAPI GatewayのIPアドレスを許可するルールを追加する
2.Amazon API GatewayでREST APIを作成し、API GatewayのVPCリンクを作成する
3.Amazon API GatewayでREST APIを作成し、API GatewayとEC2の間にNATゲートウェイを設定する
4.Amazon API GatewayでWebSocket APIを作成し、API GatewayのVPCリンクを作成する

### 正解
2
### 自分の回答
3

### 問題
ある会社では、Amazon RDS MySQLデータベースを使用した新しいウェブアプリケーションのバックエンドを設計している。データベースに障害が発生した場合でも、迅速にフェイルオーバーでき、サービスの中断やデータの損失を最小限に抑えられる可用性の高いソリューションを探している。また、アプリケーションのRPO（目標復旧時点）は1秒未満を目標としている。これらの要件を満たす最適なソリューションはどれか。
1.RDS MySQLデータベースのスナップショットを取得するAWS Lambda関数を作成し、定期的に実行する
2.RDS MySQLデータベースのリードレプリカを複数AZ上に配置する
3.Amazon ElastiCacheを構成し、RDS MySQLデータベースと連携させる
4.RDS MySQLデータベースのマルチAZ配置を使用する

### 正解
4
### 自分の回答
2

### 問題
ある企業では、開発用と本番用にそれぞれ1つずつ、合計2つのVPCを展開している。これら2つのVPC間ではVPCピアリング接続が構成されており、相互通信が可能である。開発用VPCは、オンプレミスのデータセンターとAWS Site-to-Site VPNを介して通信しており、データセンターには単一のカスタマーゲートウェイが配置されている。本番用VPCは、オンプレミスのデータセンターとAWS Direct Connectを介して通信しており、複数のDirect Connectが構成されている。同社は、この構成における単一障害点をなくし、可用性を高めたいと考えている。どのような対策をとれば良いか。
1.開発用VPCと本番用VPC間のVPCピアリング接続を複数設定して、相互通信の可用性を高める
2.オンプレミスのデータセンターに新たにカスタマーゲートウェイを追加し、Site-to-site VPN接続の冗長性を確保する
3.本番用VPCのDirect Connectの代わりに、Site-to-Site VPNを設定する
4.開発用VPCのSite-to-Site VPNのVPNトンネルを複数設定し、VPN接続の冗長性を確保する

### 正解
2
### 自分の回答
4

### 問題
ある企業では、オンプレミス上で請求データファイルを集計・分析するバッチ処理が毎週同時刻に実行されている。請求データはオンプレミスのファイルシステムが既存のSFTPサーバー経由で受け取っている。同社は、既存のSFTPサーバーでの受け取りを含む一連の処理をAWSに移行することで、高可用性と耐障害性を実現し、運用上のオーバーヘッドも最小限に抑えたいと考えている。どの解決策が適切か。
1.EC2インスタンス上にSFTPサーバーをデプロイし、請求データファイルを受け取る。ストレージとしてEBSを使用し、スケジュールされたスケーリングポリシーを持つAuto Scalingグループに属するEC2インスタンスでEBSからファイルを読み込み、バッチ処理を実行する
2.Transfer for SFTPを使用して請求データファイルを受け取り、EFSに保存する。スケジュールされたスケーリングポリシーを持つAuto Scalingグループに属するEC2インスタンスにオンプレミスのアプリケーションを移行し、EFSにアクセスしてバッチ処理を実行する
3.AWS Glueを使用してオンプレミスのファイルシステムからS3にデータを取り込む。スケジュールされたスケーリングポリシーを持つAuto Scalingグループに属するEC2インスタンスにオンプレミスのアプリケーションを移行し、S3バケットからファイルをダウンロードしてバッチ処理を実行する
4.オンプレミスのSFTPサーバーからDataSync経由で請求データファイルをS3バケットに転送する。スケジュールされたスケーリングポリシーを持つAuto Scalingグループに属するEC2インスタンスにオンプレミスのアプリケーションを移行し、S3バケットからファイルをダウンロードしてバッチ処理を実行する

### 正解
2
### 自分の回答
4

### 問題
ある製造業の社内在庫管理システムは、ALBの背後にあるAuto ScalingグループのEC2インスタンス上で稼働している。このシステムは、平日9:00〜18:00の業務時間にトラフィックが集中し、CPU使用率が上昇する。一方で、土日は業務が停止し、システムを稼働させる必要がない。ソリューションアーキテクトは、コストを抑えつつ業務時間中のパフォーマンスを維持するために、どのアクションを組み合わせて実施すべきか。（2つ選択）
1.Auto Scalingのライフサイクルフックを利用して、休日にインスタンスを自動的に終了させる
2.スケジュールに基づくスケーリングを設定し、土日にはAuto Scalingグループの最小・最大・希望容量を0にし、週明けに元の値へ戻す
3.ターゲット追跡スケーリングポリシーを使用して、インスタンスのCPU使用率に基づいてAuto Scalingグループをスケーリングする
4.リクエストレートを基準に、ALBのキャパシティを増減するようにスケーリングを設定する
5.Auto ScalingのEC2ヘルスチェックをスケーリングポリシーとして設定し、負荷に応じてインスタンス数を自動調整する

### 正解
2,3
### 自分の回答
1,2

### 問題
ある会社の開発部門では、IIS上で動作する大規模な.NETアプリケーションを開発している。このアプリケーションには高い可用性とスケーラビリティが求められているが、デプロイに際し、環境の設定や運用にかかる手間はできる限り抑えたいと考えている。これらの要件を満たす最適なソリューションはどれか。
1.EC2インスタンスを起動し、IISやランタイム、アプリケーションに必要なソフトウェアを導入後、アプリケーションをデプロイする
2.AWS Elastic Beanstalkを使用し、プラットフォームとして.NETとIISの組み合わせを選択しデプロイする
3.アプリケーションをDockerコンテナ化し、Amazon ECSにデプロイする
4.アプリケーションをAWS Lambdaを用いて実装する

### 正解
2
### 自分の回答
3

### 問題
あるニュース会社は、記者によるインタビュー音声をテキストに変換し、その内容をWebサイトに掲載する方法を検討している。その際に、インタビュー中に発言された人物の氏名や住所など個人を特定できる情報（PII：Personally Identifiable Information）を本文から削除したい。
要件を満たすために、もっとも適切なソリューションはどれか。
1.TranscribeでPIIの識別とマスキングを有効にして、音声データをテキストに変換する。出力結果をS3バケットに保存する
2.TextractでPIIの識別とマスキングを有効にして、音声データをテキストに変換する。出力結果をS3バケットに保存する
3.Translateで音声データをテキストに変換する。Comprehendでテキストを解析して、PIIをマスキングする。出力結果をEFSファイルシステムに保存する
4.Translateで音声データをテキストに変換する。MacieでテキストからPIIを抽出してマスキングし、出力結果をEFSファイルシステムに保存する

### 正解
1
### 自分の回答
4

### 問題
メディア企業は、ニュースコンテンツを世界中の視聴者に向けて配信するWebサイトを運営している。Webサイトは動的に生成される記事と画像や動画などの静的コンテンツがある。動的コンテンツはALB配下の複数のEC2インスタンス上で実行されるアプリケーションで生成され、静的コンテンツはS3バケットに保存されている。同社は視聴者のグローバル化を見据えて、静的コンテンツと動的コンテンツの配信パフォーマンスを向上させて、可能な限り低遅延で配信したいと考えている。なお、WebサイトはRoute 53に登録した独自ドメインを使用している。
世界中の視聴者に対してニュースコンテンツを低遅延で配信するために、もっとも適切な方法はどれか。
1.ALBとEC2インスタンスを世界各地のリージョンに配置する。S3バケットのクロスリージョンレプリケーションを有効にする。Route 53で地理的近接性ルーティングポリシーを使用して、独自ドメインのDNSを設定する
2.CloudFrontでALBとS3バケットをオリジンとして設定する。Route 53でCloudFrontにトラフィックをルーティングするように独自ドメインのDNSを設定する
3.CloudFrontでS3バケットをオリジンとして設定する。Global AcceleratorでALBとCloudFrontをエンドポイントとして設定する。Route 53でGlobal Acceleratorにトラフィックをルーティングするように独自ドメインのDNSを設定する
4.Global AcceleratorでALBとS3バケットをそれぞれエンドポイントとして設定する。Global Acceleratorに割り当てられたIPアドレスにトラフィックをルーティングするように、Route 53で独自ドメインのDNSを設定する

### 正解
2
### 自分の回答
1

### 問題
ある企業は、機械学習モデルを使用したリアルタイム画像認識サービスを提供している。このサービスでは、画像認識処理時に大量の参照データにアクセスし、1桁ミリ秒の極めて低いレイテンシーでの読み取りが必要である。この要件を満たすS3ストレージクラスはどれか。
1.S3 Standard
2.S3 Express One Zone
3.S3 Standard-IA（S3 Standard-Infrequent Access）
4.S3 Intelligent-Tiering

### 正解
2
### 自分の回答
4

### 問題
ある動画配信企業が、ディープラーニングを用いたリアルタイムな映像解析アプリケーションを開発している。このアプリケーションは複数のコンテナ化されたマイクロサービスで構成されており、その一部のコンポーネントでは高負荷の解析を実行するためにGPUリソースを利用する必要がある。これらの要件を満たすために最も適切な構成はどれか。
1.ECSを用いてコンテナをオーケストレーションし、GPUリソースを必要とするコンポーネントをFargate起動タイプで実行する
2.コンテナイメージをECRに格納し、GPUリソースを必要とするコンポーネントをECRで直接実行する
3.ECSを用いてコンテナをオーケストレーションし、GPUリソースを必要とするコンポーネントをGPU対応のEC2インスタンスで実行する
4.GPUリソースを必要とするジョブをBatchのジョブキューに送信し、GPU対応のEC2インスタンスで実行する
5.GPU対応のEC2インスタンスで実行する

### 正解
3
### 自分の回答
1

### 問題
ある企業は、オンライン学習プラットフォームをEC2インスタンス群で運用している。このプラットフォームでは、教育用の画像ファイルやPDF資料が数千点保存されており、世界中の受講生がアクセスする。現在、これらのファイルはEC2インスタンスのローカルストレージに保存されているが、トラフィックの増加に伴い、EC2インスタンスの負荷が高くなっている。ソリューションアーキテクトは、EC2インスタンスの負荷を軽減し、グローバルなユーザーに対してコンテンツを低レイテンシーで配信するソリューションを設計する必要がある。最も適切なソリューションはどれか。
1.EFSファイルシステムにファイルを保存する。複数のEC2インスタンスから同時にアクセスできるようにマウントする
2.S3バケットにファイルを保存し、S3の静的Webサイトホスティング機能を使用してコンテンツをグローバルに配信する
3.EBSボリュームにファイルを保存し、複数のEC2インスタンスから同時にアクセスできるようにする。プロビジョンドIOPS SSD（io2）を使用してパフォーマンスを向上させる
4.S3バケットにファイルを保存し、OACを設定したCloudFront経由で配信する

### 正解
4
### 自分の回答
1

### 問題
ある企業は、自社のカスタマーサポートセンター向けに問い合わせチケットを管理するシステムを構築している。各チケットはスレッド形式のメッセージ一覧、添付ファイルのメタデータ、顧客プロフィールなどをネストしたドキュメント構造で保存し、トラフィック増大時でも低レイテンシで応答できることが求められている。また、顧客がメッセージ内に誤って住所や電話番号などの個人識別情報（PII）を入力してしまうリスクがあるため、毎月全データをスキャンし、PIIが検出された場合にはコンプライアンス部門へメールで通知したい。これらの要件を満たすために、ソリューションアーキテクトが取るべき方法の組みあわせは次のうちどれか。（2つ選択）
1.Macieを有効化して、月次スケジュールの分類ジョブを設定してスキャンを実行する。ジョブ完了イベントをEventBridgeで捕捉してSNSトピックに転送する
2.Macieを有効化して、月次の分類ジョブ完了イベントをEventBridgeで捕捉してLambdaに送信する
3.DynamoDBにチケットをネスト構造で保存し、エクスポート機能で毎月S3に出力する
4.S3にJSONファイルでチケットを保存し、Athenaでクエリを実行する
5.DynamoDB Streamsを有効化して書き込みイベントをLambdaに送り、各レコードでComprehendを実行してPIIを検出し、SNSで通知する

### 正解
1,3
### 自分の回答
3,5

### 問題
ある企業は、Amazon Linuxを実行する複数のEC2インスタンス上で、社内向けの画像変換アプリケーションを開発している。このアプリケーションでは、変換処理の実行中に一時ファイルを保存する。一方で、変換後の画像ファイルは長期間保持しなければならない。変換後の画像ファイルは、どのEC2インスタンスからでも同じ内容を参照できる必要があり、ストレージ障害によって利用できなくなるリスクは最小限に抑えなければならない。さらに、アプリケーションが利用する保存先はいずれも高いパフォーマンスを提供し、Linux環境からネイティブに扱えるものでなければならない。これらの要件を最も適切に満たす構成はどれか。(2つ選択)
1.Amazon ElastiCache for Memcachedを構成し、一時データの保存先として使用するように EC2インスタンスを設定する
2.長期データの保存先として、AWS Storage Gatewayのボリュームゲートウェイ（保管型）を構成する
3.長期データの保存先として、各EC2インスタンスに個別のEBSボリュームをアタッチする
4.長期データの保存先として、Amazon EFSのリージョンファイルシステムを構成する
5.一時データの保存先として、イ長期データの保存先として、AWS Storage Gatewayのボリュームゲートウェイ（保管型）を構成する
6.長期データの保存先として、各EC2インスタンスに個別のEBSボリュームをアタッチする
7.長期データの保存先として、Amazon EFSのリージョンファイルシステムを構成する
8.一時データの保存先として、インスタンスストアをサポートする EC2インスタンスタイプを使用する

### 正解
4,5
### 自分の回答
2,5

### 問題
ある会社は、EC2インスタンスで稼働する勤怠管理アプリケーションを構築した。同社は既存のAWS Direct Connect回線を利用している。勤怠管理アプリケーションは、毎月1か月分の勤怠実績を集計し、全社員分の勤怠データを作成する。作成された勤怠データは暗号化して社員に配布する。データは1件あたり平均10MBで、配布する勤怠データの月合計は約500GB以上になる。
データ転送コストを削減するために、もっとも適切なソリューションはどれか。
1.勤怠管理アプリケーションからS3バケットへサーバー側の暗号化を利用してデータをアップロードする。社員はインターネット経由でS3バケットから勤怠データをダウンロードする
2.社員はインターネット経由で勤怠管理アプリケーションにHTTPS接続して勤怠データをダウンロードする
3.勤怠管理アプリケーションからDirect Connectを経由して、オンプレミスのファイルサーバーへ勤怠データをアップロードする。社員はファイルサーバーからダウンロードする
4.勤怠管理アプリケーションからSFTP接続でNATゲートウェイを経由して、オンプレミスのファイルサーバーへ勤怠データをアップロードする。社員はファイルサーバーからダウンロードする

### 正解
3
### 自分の回答
1

### 問題
ある金融サービス会社は、顧客情報のレポート処理システムをAWSクラウドに移行するプロジェクトを進めている。大量の取引データを処理して日次レポートを生成するアプリケーションを、EC2インスタンスに移行する予定である。EC2インスタンスで処理後、日次レポートはS3バケットに保存する。日次レポートは厳格なセキュリティとプライバシー要件に従って管理されなければならず、データの外部への露出を最小限に抑えることが重要である。
データの保存と取得に関するコストの削減を求める場合、選択すべき手段はどれか。
1.EC2インスタンスのあるVPCに、ゲートウェイ型VPCエンドポイントを作成する。エンドポイントへのルーティングをルートテーブルに設定し、S3バケットへアクセスする
2.パブリックサブネットにNATゲートウェイを作成する。NATゲートウェイへのルーティングをルートテーブルに設定し、S3バケットへアクセスする
3.EC2インスタンスのあるVPCに、インターネットゲートウェイを作成する。インターネットゲートウェイへのルーティングをルートテーブルに設定し、S3バケットへアクセスする
4.EC2インスタンスのあるVPCに、PrivateLinkを作成する。エンドポイントへのルーティングをルートテーブルに設定し、S3バケットへアクセスする

### 正解
1
### 自分の回答
2

### 問題
スタートアップ企業が製品紹介用の静的ウェブサイトを運営している。主な訪問者は北米（米国・カナダ）と日本のユーザーである。ページは画像とHTMLだけで構成され、アップロード頻度も低いため書き込み性能や高可用性はあまり重視していない。コストを最小限に抑えつつ、両地域のユーザーに低遅延でコンテンツを配信するために、最も適切なアーキテクチャはどれか。
1.ウェブサイトのすべてのファイルをus-west-2リージョンのS3バケットに配置し、全世界のエッジロケーションを使用するよう料金クラスを設定したCloudFrontディストリビューションを構成する
2.ウェブサイトのすべてのファイルをus-west-2リージョンとap-northeast-1リージョンのS3バケットに配置し、単一のCloudFrontディストリビューションを作成する。Lambda@Edgeのイベントでユーザーの地域を判定し、最も近いリージョンのエッジロケーションにリクエストをルーティングするよう関数を実装する
3.ウェブサイトのすべてのファイルをus-west-2リージョンのS3バケットに配置し、制限されたエッジロケーションを利用するよう料金クラスを設定したCloudFrontディストリビューションを構成する
4.ウェブサイトのすべてのファイルをus-west-2リージョンとap-northeast-1リージョンのS3バケットに配置し、それぞれをオリジンとする。2つのCloudFrontディストリビューションを作成したうえで、Route 53のレイテンシールーティングポリシーにより最適なディストリビューションにトラフィックを振り分ける

### 正解
3
### 自分の回答
4

### 問題
自動車販売会社は、あるAWSアカウントに営業用のVPC、別のアカウントに商品管理用のVPCを保有している。販売員は営業用VPCのプライベートサブネット内にある営業支援ツールから、商品管理用VPC内のALB背後にある見積書作成アプリケーションへNATゲートウェイ経由でアクセスする。見積書作成アプリケーションで作成されるデータは1件あたり約5MBで、作成後に自動で営業支援ツールへ送信される。リクエストは1日に数千件あり、会社ではデータ転送コストが高いことが問題となっている。
どのようなソリューションがデータ転送コストを削減できるか。（2つ選択）
1.営業用VPCと商品管理用VPCをAWS Direct Connectで接続する
2.商品管理用VPCをAWS Resource Access Managerで共有する
3.営業支援ツールをオンプレミスに配置して、オンプレミスからインターネット経由でALBへアクセスする
4.営業用VPCと商品管理用VPCをVPCピアリングで接続する
5.営業用VPCからPrivateLinkで商品管理用VPCのALBへアクセスする

### 正解
2,4
### 自分の回答
4,5

### 問題
ある企業では、EC2インスタンスに接続されたEBSボリュームのデータ保護の一環として、これまで必要に応じて手動でスナップショットを作成してきた。しかし、明確な保持ポリシーを設けていなかったため、古いスナップショットが長期間にわたって削除されずに蓄積され、保存領域の使用量が増加し、それに伴ってストレージコストも継続的に上昇している。運用チームはこの状況を踏まえ、今後はEBSボリュームのスナップショットを毎日自動で取得し、最大30日間のみ保持する形で効率的に管理したいと考えている。これらの要件を満たしつつ、運用負荷を最小限に抑えてコスト最適化を実現するには、次のうちどの選択肢が最も適切か。
1.CloudWatchのメトリクスを用いてEBSボリュームのスナップショット保存容量をモニタリングし、閾値を超えたときにEventBridgeルールでSNS通知を発行して運用担当者にアラートを送り、通知を受けた担当者がスナップショットを削除する
2.S3のライフサイクルポリシーを設定して、30日を超えたスナップショットを自動的に削除するように構成する
3.Data Lifecycle Managerを使用して、30日間の保持ルールを持つスナップショットポリシーを作成し、今後のバックアップ対象となるEBSボリュームに適用する
4.CLIを用いて毎日スナップショットを作成し、30日を超えた古いスナップショットを削除するシェルスクリプトを作成し、EC2インスタンス上のcronにより定期実行する

### 正解
3
### 自分の回答
2

### 問題
ある企業は、マイクロサービスベースの本番アプリケーションをAWS上で運用している。フロントエンドおよび一部のAPIはAuto Scalingグループ内のEC2インスタンスで稼働しており、画像処理やバッチ処理などのバックグラウンドジョブはLambda関数として実装されている。このアプリケーションは少なくとも今後1年以上は継続運用する計画であり、将来的な機能追加やアーキテクチャの見直しによって、EC2インスタンスとLambda関数の利用比率が変動する可能性がある。そのため、特定のインスタンス構成に固定されることなく、継続的にコスト削減効果を得られる購入オプションを選択したい。
1.1年間のEC2 Instance Savings Plansを全額前払いで購入する
2.1年間のCompute Savings Plansを全額前払いで購入する
3.EC2インスタンスはすべてスポットインスタンスに変更し、Lambdaにはオンデマンド料金を適用する
4.1年間のコンバーティブルリザーブドインスタンスを全額前払いで購入する

### 正解
2
### 自分の回答
1
